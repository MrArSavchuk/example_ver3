# Contributing Guide

Спасибо, что участвуешь в разработке проекта **Olesya Martin Designer** 🙌  
Этот документ описывает базовые правила и принципы работы с кодом.

Цель — чтобы проект оставался:
- понятным
- масштабируемым
- предсказуемым при изменениях

---

## Архитектура

Проект построен по подходу **FSD (Feature-Sliced Design)**.

Основная идея:
- код группируется по смыслу, а не по типу файлов
- фичи изолированы и не знают лишнего
- UI не содержит бизнес-логики

Если ты не уверен, куда положить код — **это повод обсудить**, а не «положить куда получилось».

---

## Mobile First

Проект проектируется и реализуется по принципу **Mobile First**.

Это означает:
- сначала проектируем и верстаем мобильную версию
- затем масштабируем под планшет и десктоп
- не «урезаем» десктоп под мобильный, а наоборот

При добавлении нового UI:
- проверь поведение на мобильном экране
- избегай фиксированных размеров без необходимости

---

## Shared UI и сторонние библиотеки

В проекте используются сторонние UI-библиотеки
(уведомления, слайдеры, пагинация, иконки и т.д.)
только через собственные обёртки в `shared/ui`.

В проекте допускается использование внутренних библиотек,
разработанных командой проекта.

Для React-хуков используется библиотека  
**@techlabteam/useful-hooks** как предпочтительный источник
повторно используемой логики.

Перед созданием нового хука необходимо проверить,
не решается ли задача существующим хуком из библиотеки.

### Общие правила

- использование UI-библиотек должно быть осознанным и согласованным
- проект стремится к единому визуальному и UX-стилю
- не допускается произвольное подключение альтернативных UI-решений

### Документация

Правила использования и ограничения для конкретных UI-библиотек
описаны в папке `docs/ui`.

### Запрещено

- подключать UI-библиотеки, не зафиксированные в документации
- дублировать UI-паттерны
- менять визуальное или поведенческое решение без обсуждения

---


## Обсуждения и изменения

Если ты:
- меняешь архитектурный подход
- добавляешь новый паттерн
- ломаешь существующее решение

→ **зафиксируй это в `DECISIONS.md`**.

### 📝 Как добавлять новые решения

Каждое новое архитектурное решение оформляется отдельным разделом:

- контекст
- принятое решение
- причины
- последствия

Если решение временное — это явно указывается.

Этот файл — **живая история проекта**, а не формальность.

Проект развивается осознанно, а не случайно.

---

## Интернационализация (i18n)

Проект поддерживает **два языка** и использует централизованную систему интернационализации (**i18n**).

### Общие правила

- **Любой пользовательский текст** должен быть вынесен в систему переводов
- В коде **запрещены захардкоженные строки**, кроме:
  - технических сообщений
  - временных отладочных подписей (до финальной версии)

### Структура переводов

- переводы организованы по смыслу, а не по страницам
- ключи должны быть:
  - осмысленными
  - стабильными
  - не зависящими от конкретного языка
- добавление нового текста требует наличия переводов
  для **всех поддерживаемых языков**

### Изменения текстов

- изменение пользовательского текста считается архитектурным изменением
- удаление ключей сопровождается удалением переводов
- временные тексты явно помечаются

i18n — часть архитектуры проекта, а не деталь реализации.

---

### Импорты и структура модулей

В проекте используется паттерн **barrel files**  
для формирования публичного API папок.

- импорты выполняются только через `index.ts`
- прямые импорты из внутренних файлов запрещены

Подробные правила и примеры описаны в:
`docs/guidelines/barrel-files.md`

---


